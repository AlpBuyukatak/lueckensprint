const assert=require('assert');
const {heatmapData,activityForDate}=require('./app.js');
const data=heatmapData(35);assert.equal(data.length%7,0,'heatmap must contain complete weekday columns');assert.ok(data.some(day=>day.today),'today must be highlighted');assert.ok(data.some(day=>day.future),'future dates must be distinguishable');assert.ok(data.every(day=>day.weekday>=0&&day.weekday<7),'weekday alignment is invalid');assert.ok(data.some((day,index)=>index&&day.month!==data[index-1].month),'month transitions must be represented');const activity=activityForDate(data.find(day=>day.today).date);for(const key of ['texts','gaps','correct','accuracy','seconds','simulations','target','intensity'])assert.ok(key in activity,`activity tooltip data lacks ${key}`);assert.equal(heatmapData(365).length%7,0,'year heatmap must remain week-aligned');console.log('Activity heatmap generation and tooltip-data tests passed');
